import FAQPage from "./components/FAQPage";

export const metadata = {
  title: "FAQs | Magic Collar",
  description:
    "Find answers about Magic Collar fitment, orders, payments, shipping, pickup, returns, and reseller inquiries.",
};

const Page = () => {
  return <FAQPage />;
};

export default Page;
